# Test Document

This is a regular paragraph to test if text rendering works correctly.

## LaTeX Tests

Here is an inline formula: $E = mc^2$.

Here is a block formula:
$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

Here is a formula with text containing spaces:
$$
f(x) = \text{Value if } x > 0
$$
