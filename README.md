# Markdown to DOCX Converter

Десктопное приложение для конвертации Markdown файлов в DOCX с поддержкой LaTeX формул.

## ✨ Возможности

- 📄 Конвертация Markdown → DOCX
- 🔬 Поддержка LaTeX формул ($...$ и $$...$$)
- ⚙️ Настройка параметров форматирования
- 🎨 Современный UI на React + TailwindCSS
- 🖥️ Кроссплатформенное (Windows, macOS, Linux)

## 📋 Требования

- Node.js 18+
- Python 3.10+
- LaTeX (для рендеринга формул)

## 🚀 Установка

```bash
# 1. Клонируйте репозиторий
cd markdown-converter

# 2. Установите Node.js зависимости
npm install

# 3. Установите Python зависимости
cd python
pip install -r requirements.txt
cd ..

# 4. Установите LaTeX (опционально, для формул)
# Ubuntu/Debian:
sudo apt-get install texlive-full

# macOS:
brew install mactex-no-gui

# Windows:
# Скачайте с https://miktex.org/
```

## 🎮 Запуск

### Режим разработки

```bash
npm run dev
```

Запустит React (localhost:5173) и Electron одновременно.

### Продакшен сборка

```bash
npm run dist
```

Создаст исполняемые файлы для вашей платформы.

## ⚙️ Настройки

| Параметр | Значение по умолчанию |
|----------|----------------------|
| Шрифт | Times New Roman, 14pt |
| Интервал | 1.5 (полуторный) |
| Красная строка | 1.27 см |
| Поля | Верх 2, Низ 2, Лево 3, Право 1.5 см |

## 📝 Поддерживаемые элементы Markdown

- Заголовки H1-H6
- Параграфы
- Нумерованные списки (1., 2., 3.)
- Маркированные списки (•)
- Жирный текст (**text**)
- Курсивный текст (*text*)
- Цитаты (> quote)
- Инлайн код (`code`)
- Блок код
- LaTeX формулы ($E=mc^2$, $$\int...$$)

## 🏗️ Структура проекта

```
markdown-converter/
├── electron/           # Electron main process
│   ├── main.js
│   └── preload.js
├── python/             # Конвертер
│   ├── md2docx.py
│   └── requirements.txt
├── src/                # React components
│   ├── components/
│   │   ├── FileUploader.jsx
│   │   ├── Settings.jsx
│   │   └── Converter.jsx
│   ├── App.jsx
│   └── index.css
├── public/
│   └── index.html
├── package.json
├── vite.config.js
└── tailwind.config.js
```

## 📦 Сборка для дистрибуции

```bash
# Windows
npm run dist  # Создаст .exe в dist/

# macOS
npm run dist  # Создаст .dmg в dist/

# Linux
npm run dist  # Создаст .AppImage в dist/
```

## 🐛 Устранение проблем

### LaTeX формулы не рендерятся
- Убедитесь, что установлен texlive-full
- Проверьте, что matplotlib может использовать usetex=True

### Ошибка "python3 not found"
- Убедитесь, что Python 3 установлен
- Проверьте PATH переменную

### Electron окно не открывается
- Запустите в режиме разработки: `npm run dev`
- Проверьте консоль на ошибки

## 📄 Лицензия

MIT
